const defaultState = {

}

export const weatherReducer = (state = defaultState, action) => {
    switch (action.type) {
        case :

            return {...state, };
        default:
            return state;
    }
}