const defaultState = {

}

export const cityReducer = (state = defaultState, action) => {
    switch (action.type) {
        case :

            return {...state, };
        default:
            return state;
    }
}